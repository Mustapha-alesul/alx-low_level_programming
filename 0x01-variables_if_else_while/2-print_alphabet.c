#include <stdio.h>
/**
 * main -a program that prints the alphabet in lowercase
 * Return:o value
 */
int main(void)
{
	char m;

	for (m = 'a'; m <= 'z'; m++)
	{
	putchar(m);
	}
	putchar('\n');
	return (0);
}
